@Styles.Render("~/Bundles/DateTimePickerStyle")
<div class="dp-date-picker">
    <div class="dp-date-control">
        <span class="glyphicon glyphicon-chevron-left dtp-clickable dp-month-backwards" aria-hidden="true" ng-click="moveMonth(-1)"></span>
        <span class="dp-date-display">{{displayDate}}</span>
        <span class="glyphicon glyphicon-chevron-right dtp-clickable dp-month-forwards" aria-hidden="true" ng-click="moveMonth(1)"></span>
    </div>
    <table class="dp-calendar">
        <thead>
            <tr>
                <th class="dp-calendar-column" ng-repeat="weekday in weekdays">{{weekday}}</th>
            </tr>
        </thead>
        <tbody>
            <tr ng-repeat="row in rows">
                <td class="dtp-clickable dp-calendar-cell" ng-click="setModelDate(cell.date)" ng-class="{'dp-calendar-cell-selected' : cell.selected}" ng-repeat="cell in row">
                    {{cell.date.date()}}
                </td>
            </tr>
        </tbody>
    </table>
</div>
