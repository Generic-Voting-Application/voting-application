<div class="account-login dialog-content centered">
    <form name="loginForm" ng-submit="loginAccount(loginForm)">
        <div class="form-section-block centered">
            <label for="email">Email</label>
            <input autofocus required class="form-input" type="email" id="email" placeholder="user@example.com" ng-model="loginForm.email">

            <label for="password">Password</label>
            <input type="password" required class="form-input" id="password" placeholder="Password" ng-model="loginForm.password">
        </div>
        <button class="active-btn" ng-disabled="loginForm.$invalid" type="submit">Sign In</button>
        <button class="inactive-btn" type="button">Forgot Password?</button>
    </form>
</div>